# 阅片并发测试 - 服务器部署说明

## 一、需要拷贝到服务器的文件

只需要这 5 个文件（不要拷 `.venv` 和 `__pycache__`）：

```
engine.py          # 测试引擎
server.py          # Web 服务
index.html         # 可视化面板
config.json        # 配置（账号、接口地址）
requirements.txt   # 依赖清单
```

## 二、服务器环境要求

- Linux（CentOS 7+/Ubuntu 18+）或 macOS
- Python 3.9 及以上
- 能访问被测系统 `192.168.108.109:8000`（内网互通）
- 内存建议 32GB+（跑 100 个浏览器上下文）

## 三、部署步骤

### 1. 上传文件

```bash
# 在服务器上创建目录
mkdir -p ~/peizhi

# 从你本机上传（在本机执行）
scp engine.py server.py index.html config.json requirements.txt 用户名@服务器IP:~/peizhi/
```

### 2. 安装 Python 依赖

```bash
cd ~/peizhi

# 创建虚拟环境
python3 -m venv .venv
source .venv/bin/activate

# 安装依赖（国内服务器用清华镜像加速）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 安装 Playwright 浏览器（Chromium）
playwright install chromium
```

> 如果 `playwright install chromium` 下载慢，可设置镜像：
> ```bash
> export PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright
> playwright install chromium
> ```

### 3. 启动服务

```bash
cd ~/peizhi
source .venv/bin/activate
python server.py
```

服务启动后监听 **9000 端口**。

### 4. 访问可视化面板

浏览器打开：`http://服务器IP:9000/`

在面板上点「开始测试」即可。

## 四、后台运行（关闭 SSH 后不中断）

```bash
cd ~/peizhi
source .venv/bin/activate
nohup python server.py > server.log 2>&1 &
```

查看日志：`tail -f server.log`
停止服务：`pkill -f "python server.py"`

## 五、防火墙放行 9000 端口（如需要）

```bash
# CentOS
firewall-cmd --add-port=9000/tcp --permanent && firewall-cmd --reload

# Ubuntu
sudo ufw allow 9000/tcp
```

## 六、注意事项

1. **内存**：100 个浏览器上下文约需 5~10GB 内存，建议服务器 32GB+
2. **网络**：服务器最好与被测系统 `192.168.108.109` 同内网，测出的数据更干净
3. **账号**：`config.json` 里已配好 100 个账号（test、test2~test100）
4. **超时**：100 并发下服务器响应慢，面板上「图像加载超时上限」建议设 180 秒
